#sub_path_only rewrite ^__PATH__$ __PATH__/ permanent;
location __PATH__/ {

  # Path to source
  alias __INSTALL_DIR__/public/;

  index index.php;

  # Common parameter to increase upload size limit in conjunction with dedicated php-fpm file
  client_max_body_size __PHP_UPLOAD_MAX_FILESIZE__;

  # Moodle 5.1+ front-controller router: serve real files/directories directly,
  # and route everything else to r.php. We use `if (!-e ...)` + `rewrite ... last`
  # (a documented-safe use of `if`) instead of `try_files ... /r.php`, because a
  # try_files fallback does NOT fire reliably in a location that uses `alias`
  # (it returns nginx's own 404 rather than reaching r.php).
  if (!-e $request_filename) {
    rewrite ^ __PATH__/r.php last;
  }

  location ~ [^/]\.php(/|$) {
    fastcgi_pass unix:/var/run/php/php__PHP_VERSION__-fpm-__APP__.sock;
    include fastcgi_params_with_auth;
  }
  # Hide all dot files but allow "Well-Known URIs" as per RFC 5785 
location ~ /\.(?!well-known).* {
    return 404;
} 
  # Don't allow direct access to various internal files. See MDL-69333 
location ~ (/vendor/|/node_modules/|composer\.json|/readme|/README|readme\.txt|/upgrade\.txt|/UPGRADING\.md|db/install\.xml|/fixtures/|/behat/|phpunit\.xml|\.lock|environment\.xml) { 
    deny all;
    return 404; 
  }
}
